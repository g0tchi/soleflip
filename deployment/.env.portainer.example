# Soleflip Optimized Stack Environment Variables for Portainer/Synology NAS
# Copy this file to .env and update the values
# Stack: PostgreSQL 17, n8n, Metabase, Redis, Adminer

# =============================================================================
# SECURITY - CHANGE ALL THESE VALUES!
# =============================================================================

# PostgreSQL Password (use a strong password!)
POSTGRES_PASSWORD=your_very_secure_postgres_password_here

# n8n Encryption Key (generate with: openssl rand -hex 32)
N8N_ENCRYPTION_KEY=your_n8n_encryption_key_32_characters

# Redis Password (required for n8n queue management)
REDIS_PASSWORD=your_secure_redis_password

# =============================================================================
# GENERAL SETTINGS
# =============================================================================

# Timezone (adjust to your location)
TIMEZONE=Europe/Berlin

# =============================================================================
# n8n SETTINGS
# =============================================================================

# n8n Host (use your NAS IP or domain)
N8N_HOST=192.168.1.100
# or: N8N_HOST=nas.local

# n8n Webhook URL (use your NAS IP or domain)
N8N_WEBHOOK_URL=http://192.168.1.100:5678/
# or for external access with domain:
# N8N_WEBHOOK_URL=https://n8n.yourdomain.com/

# =============================================================================
# NOTES
# =============================================================================

# Service Access URLs (adjust IP to your NAS):
# - n8n Workflow Automation: http://192.168.1.100:5678
# - Metabase Analytics: http://192.168.1.100:6400
# - Adminer DB Manager: http://192.168.1.100:8220
# - PostgreSQL 17 Direct: 192.168.1.100:5432

# Resource Limits (per container):
# - PostgreSQL 17: 2GB RAM, 2 CPUs
# - n8n: 1GB RAM, 1.5 CPUs
# - Metabase: 1.5GB RAM, 1.5 CPUs
# - Redis: 256MB RAM, 0.5 CPU
# - Adminer: 256MB RAM, 0.25 CPU
# Total: ~5GB RAM, ~5.75 CPUs (max)
# Idle: ~1.5GB RAM, ~1.5 CPUs

# To generate secure keys:
# - Encryption keys: openssl rand -hex 32
# - JWT secrets: openssl rand -base64 32
# - Passwords: pwgen -s 32 1 (or use a password manager)

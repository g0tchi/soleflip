# PostgreSQL Database Configuration
# Uncomment and update with your PostgreSQL connection details

# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/soleflip_db

# Example for local development:
# DATABASE_URL=postgresql+asyncpg://postgres:yourpassword@localhost:5432/soleflip

# Example for production:
# DATABASE_URL=postgresql+asyncpg://soleflip_user:secure_password@your-postgres-server:5432/soleflip_production

# If DATABASE_URL is not set, the system will use SQLite for demo mode
# For production, always set DATABASE_URL to your PostgreSQL connection string

# Optional: Enable SQL query logging (development only)
# SQL_ECHO=true